name: Enable Maintenance Mode

on:
  workflow_dispatch:

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate maintenance mode
        run: |
          echo "Putting site into maintenance mode..."
          sleep 3
          echo "Maintenance mode enabled."
